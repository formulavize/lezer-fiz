# Empty



==>

Recipe()

# Two Alias Statements

y = x
z = y

==>

Recipe(
  Alias(Variable, Variable),
  Alias(Variable, Variable)
)

# Mixed Statements

y = f(x)
x = y
g()

==>

Recipe(
  Assignment(Variable, Call(ArgList(Variable)))
  Alias(Variable,Variable),
  Call(ArgList)
)

# Line Comment

// Comment

==>

Recipe(LineComment)

# Block Comments

/* OneLine */
/*
  Newline
*/
/*/*DoubleStart*/
/***
  Stars
***/

==>

Recipe(BlockComment, BlockComment, BlockComment, BlockComment)

# Mixed Statements with Comments

// Comment
y = f(x)
/* Comment */
x = y
/*
Comment
*/
g()

==>

Recipe(
  LineComment
  Assignment(Variable, Call(ArgList(Variable)))
  BlockComment
  Alias(Variable,Variable),
  BlockComment
  Call(ArgList)
)

# Semicolons

y = f(x);
x = y;
g();

==>

Recipe(
  Assignment(Variable, Call(ArgList(Variable)))
  Alias(Variable,Variable),
  Call(ArgList)
)

# Semicolons Same Line

a=b();f();x=y;

==>

Recipe(
  Assignment(Variable, Call(ArgList)),
  Call(ArgList),
  Alias(Variable, Variable)
)

# Semicolon Same Line Divider

y = f(
  x,
  y,
  z
) ; a = b

==>

Recipe(
  Assignment(Variable, Call(ArgList(Variable, Variable, Variable))),
  Alias(Variable, Variable)
)

# Diagram

         x = f(i); y = g(j) 
//       |         |
   m = a(x); n = b(y)
//  \       /
//   \     /
//    \   /
   foo(m,n)

==>

Recipe(
  Assignment(Variable, Call(ArgList(Variable))),
  Assignment(Variable, Call(ArgList(Variable))),
  LineComment,
  Assignment(Variable, Call(ArgList(Variable))),
  Assignment(Variable, Call(ArgList(Variable))),
  LineComment,
  LineComment,
  LineComment,
  Call(ArgList(Variable, Variable))
)
